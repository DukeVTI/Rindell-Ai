═══════════════════════════════════════════════════════════════════════════
  🎯 YOUR APP IS CRASHING? DO THIS IMMEDIATELY! 🎯
═══════════════════════════════════════════════════════════════════════════

STEP 1: PULL LATEST FIXES
──────────────────────────────────────────────────────────────────────────
cd ~/rindell/Rindell-Ai
git pull origin copilot/na

STEP 2: RUN DIAGNOSTICS (FINDS THE PROBLEM AUTOMATICALLY!)
──────────────────────────────────────────────────────────────────────────
npm run diagnose

This will check EVERYTHING and tell you EXACTLY what's wrong!

STEP 3: FOLLOW THE FIX IT SHOWS
──────────────────────────────────────────────────────────────────────────
The diagnostic tool will show you specific commands to fix the issue.

MOST COMMON FIX (if node_modules is missing):
──────────────────────────────────────────────────────────────────────────
rm -rf node_modules package-lock.json
npm cache clean --force
npm install
ls node_modules/ | head -20  # VERIFY you see packages!
pm2 restart rindell-mvp
pm2 logs rindell-mvp --lines 20

EXPECTED SUCCESS OUTPUT:
──────────────────────────────────────────────────────────────────────────
After npm install, you should see:
  "added 621 packages, and audited 622 packages"
  
After ls node_modules/, you should see:
  @babel
  @hapi
  @whiskeysockets
  axios
  bcrypt
  ... (many more packages)

After pm2 logs, you should see:
  ✅ Configuration validated
  ✅ Database connected successfully
  ✅ Queue service initialized
  ✅ Server started on port 3000

═══════════════════════════════════════════════════════════════════════════
                      IF DIAGNOSTICS SHOWS ISSUES
═══════════════════════════════════════════════════════════════════════════

CRITICAL ISSUE: node_modules directory missing
→ Read: NODE-MODULES-MISSING.txt (comprehensive guide)
→ Quick fix: rm -rf node_modules && npm install

ISSUE: Missing .env variables  
→ Edit: nano .env
→ Set: DB_PASSWORD, GROQ_API_KEY, JWT_SECRET

ISSUE: PostgreSQL not running
→ Start: sudo systemctl start postgresql

ISSUE: Redis not running
→ Start: sudo systemctl start redis

ISSUE: Disk space low
→ Free up space: sudo apt-get clean

ISSUE: Permission problems
→ Fix: sudo chown -R $USER:$USER ~/rindell

═══════════════════════════════════════════════════════════════════════════
                         NEED MORE HELP?
═══════════════════════════════════════════════════════════════════════════

We have COMPREHENSIVE guides for every scenario:

📋 NODE-MODULES-MISSING.txt - node_modules doesn't exist
📋 STILL-NOT-WORKING.txt - npm install said "up to date"
📋 START-HERE-FIX.txt - First time setup
📋 DEPLOYMENT-READY.md - Complete deployment guide
📋 QUICK-FIX.md - Other common issues

🛠️  TOOLS:
   npm run diagnose  - Find problems automatically
   npm run preflight - Verify before deployment

═══════════════════════════════════════════════════════════════════════════
                    QUICK REFERENCE COMMANDS
═══════════════════════════════════════════════════════════════════════════

# Diagnose problems
npm run diagnose

# Check if node_modules exists
ls -la | grep node_modules

# Count packages (should be 600+)
ls node_modules/ | wc -l

# View PM2 status
pm2 status

# View PM2 logs
pm2 logs rindell-mvp --lines 30

# Restart app
pm2 restart rindell-mvp

# Stop app
pm2 stop rindell-mvp

# Delete from PM2
pm2 delete rindell-mvp

# Fresh start
pm2 delete rindell-mvp
pm2 start ecosystem.config.js

═══════════════════════════════════════════════════════════════════════════
                         ACCESS YOUR APP
═══════════════════════════════════════════════════════════════════════════

Once everything is working:

Dashboard: http://YOUR-VPS-IP:8080/dashboard.html
API:       http://YOUR-VPS-IP:3000

(Replace YOUR-VPS-IP with your actual IP address)

═══════════════════════════════════════════════════════════════════════════
