# Rindell AI Configuration

# ═══════════════════════════════════════════════════════════
# PORT CONFIGURATION
# ═══════════════════════════════════════════════════════════
# Default ports work for local development. 
# For VPS deployment with other services, change these to available ports.
#
# Quick guide:
# - Local development: Use defaults (3000, 8080)
# - VPS with domain: Use defaults, NGINX handles external access
# - VPS without domain: Change to available ports (e.g., 5000, 5001)
#
# Check port availability: sudo netstat -tlnp | grep :PORT
# See VPS-DEPLOYMENT-PORTS.md for detailed port configuration guide

# API Server Configuration (for self-hosted setup)
API_PORT=3000  # API server port (change if port in use)
API_SERVER_URL=http://localhost:3000/analyze  # API endpoint URL

# Web Dashboard Configuration
WEB_PORT=8080  # Web dashboard port (change if port in use)

# ═══════════════════════════════════════════════════════════
# EXAMPLE PORT CONFIGURATIONS FOR DIFFERENT SCENARIOS
# ═══════════════════════════════════════════════════════════

# Scenario 1: VPS with other services (ports 3000, 8080 taken)
# API_PORT=5001
# WEB_PORT=5000
# API_SERVER_URL=http://localhost:5001/analyze

# Scenario 2: Behind NGINX reverse proxy (use internal ports)
# API_PORT=3000
# WEB_PORT=8080
# API_SERVER_URL=http://localhost:3000/analyze
# (NGINX listens on public port, proxies to these internal ports)

# Scenario 3: Multiple Rindell instances
# API_PORT=4000
# WEB_PORT=4001
# API_SERVER_URL=http://localhost:4000/analyze

# ═══════════════════════════════════════════════════════════
# WHATSAPP CONFIGURATION
# ═══════════════════════════════════════════════════════════
# WhatsApp Configuration (replace with your actual WhatsApp number)
ASSISTANT_NUMBER=1234567890@c.us

# ═══════════════════════════════════════════════════════════
# GROQ AI CONFIGURATION (REQUIRED)
# ═══════════════════════════════════════════════════════════
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.1-70b-versatile

# Alternative Groq Models (uncomment to use):
# GROQ_MODEL=llama-3.1-8b-instant
# GROQ_MODEL=mixtral-8x7b-32768
# GROQ_MODEL=gemma-7b-it

# Legacy Make.com Configuration (deprecated, use API_SERVER_URL instead)
# MAKE_WEBHOOK_URL=https://hook.eu2.make.com/your_webhook_id

# Webhook Timeout (in milliseconds)
WEBHOOK_TIMEOUT=120000
